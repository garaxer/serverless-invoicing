InvoicesTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:DeleteItem
    - dynamodb:UpdateItem
  Resource:
    ${self:custom.InvoicesTable.arn}
    #- !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/InvoicesTable